Paintiano — delivery manifest
Generated: 2026-06-10T20:30:00Z
Footer: v2.0 (unchanged)
Changed: 01-core-head.jsx, 03-i18n.jsx, 04-songs.jsx, 05-main.jsx

CHANGE LOG (this delivery):
  · NEW colour mode chip: Kontra (5th chip alongside Harmony/Spectral/φ Phi/Custom).
  · Custom default rewritten → Scriabin's Prometheus colour-tone mapping (1910).
    Old "inverse-Harmony" table promoted to Kontra; new CUSTOM_DEFAULT_HUE is Scriabin.
    D♯ and A♯ desaturated to 25 % (Scriabin's "metallic" marks) via new
    CUSTOM_DEFAULT_SAT table — affects both force-seed and Reset button.
  · PALETTE_VERSION='5' → '6' → every user gets fresh Scriabin seed on first open
    after deploy (prior saved palettes discarded — same as the v4→v5 rollout).
  · UI tab pickers: 4-col → 5-col grids (image-color picker + non-image picker).
  · cycleColorFs (FS palette button): non-image cycle includes Kontra (image cycle
    still 4-tab harmony/spectral/bw/custom — Phi has never been in the FS cycle).
  · Concept 'modes' block rewritten in all 8 languages: 5 modes, Custom story now
    explicitly Scriabin / Prometheus / synaesthete, Kontra positioned as the
    painter's reading.
  · Plus accumulated changes from earlier sessions that are now in the baseline:
    Mosaic-family integration into Shuffle (Fisher-Yates seeded pool, lock mode),
    FS header with "Mosaic" / "Notes" / "inspired by One Million Dollar Page",
    canAppend oneM exclusion (Mosaic-substrate bleed-through fix).

=== FILE SIZES (line counts) ===
01-core-head.jsx:  712   (was 692; +20 for KONTRA_HUE split + CUSTOM_DEFAULT_SAT + Scriabin comments)
02-draw.jsx:      8661   (unchanged)
03-i18n.jsx:      1385   (unchanged line count; 8 kontra labels added inline)
04-songs.jsx:     1877   (unchanged line count; 8 modes-block bodies rewritten)
05-main.jsx:      9389   (was 9314; +75 for Kontra branches, Mosaic-family shuffle,
                          FS header, oneM canAppend, Fisher-Yates pool)
06-demo-reel.jsx:  227   (unchanged)
07-pro.jsx:        884   (unchanged)

=== KEY FEATURE MARKERS ===

[01-core-head.jsx]
  PHI_HUE table:                  1   (must be 1)
  phiCol fn:                      1   (must be 1)
  KONTRA_HUE table:               1   (must be 1)        [NEW — was named CUSTOM_DEFAULT_HUE pre-Scriabin]
  CUSTOM_DEFAULT_HUE (Scriabin):  1   (must be 1)        [REPURPOSED — now Scriabin's Prometheus map]
  CUSTOM_DEFAULT_SAT table:       1   (must be 1)        [NEW — per-PC saturation; D♯/A♯ at 25%]
  kontraCol fn:                   1   (must be 1)        [NEW — references KONTRA_HUE]

[02-draw.jsx]
  drawOneMOverlay:                1   (must be 1)
  _miroPal accepts gc:            7   (must be ≥4)
  variant cap branch:             2   (must be 2 — Pollock + Miró)

[03-i18n.jsx]
  phi label (8 langs):            8   (must be 8)
  kontra label (8 langs):         8   (must be 8)        [NEW]
  oneMStyle label (8 langs):      8   (must be 8)

[04-songs.jsx]
  modes block "Kontra vs Custom": 8   (must be 8)        [NEW — old "B/W vs Custom" titles gone]
  Scriabin/Skriabin in Concept:   14  (must be ≥8)       [NEW — at least one mention per language]

[05-main.jsx]
  PALETTE_VERSION='6':            1   (must be 1)        [BUMPED]
  KONTRA_HUE refs:                3   (must be 3 — stamp/image-init/image-retr) [NEW]
  CUSTOM_DEFAULT_HUE refs:        4   (must be 4 — force-seed + defaultCustomPalette + Reset button + import header)
  CUSTOM_DEFAULT_SAT refs:        6   (must be ≥3 — force-seed + defaultCustomPalette + Reset)
  kontra mode in gc():            3   (must be ≥1)       [NEW]
  kontra preview branch:          1   (must be 1)        [NEW]
  cycleColorFs includes kontra:   3   (must be ≥1)       [NEW — phi+kontra+custom triple in cycle]
  Tab picker 5-col grid:          3   (must be 3 — 2 pickers + 1 cycle helper)  [NEW]
  mosaicShuffleLock state:        5   (must be ≥3)        [from prior session — in baseline]
  MOSAIC_FAMILY shuffle:          3   (must be ≥2)        [from prior session]
  Fisher-Yates pool note:         1   (must be 1)         [from prior session]
  STYLE_INSPIRED has One Million: 1   (must be 1)         [from prior session]
  FS header bare-label branch:    1   (must be 1)         [from prior session — Mosaic/Notes show plain]
  oneM in canAppend exclusion:    1   (must be 1)         [guards Mosaic-under-oneM bleed-through]
  oneMMode state:                 1   (must be 1)
  effectiveStyle uses:            23  (must be ≥10)
  drawOneMOverlay dispatch:       3   (must be 3)
  bw→harmony useEffect:           1   (must be 1)
  Mic blob (listenBlobRef):       8   (must be ≥5)
  iOS wakeAudio:                  8   (must be ≥2)

=== HOW TO VERIFY AFTER DEPLOY ===
Run quick spot-checks in cmd (Windows, dev branch):
  findstr /C:"KONTRA_HUE" src\paintiano\01-core-head.jsx
  findstr /C:"CUSTOM_DEFAULT_SAT" src\paintiano\01-core-head.jsx
  findstr /C:"PALETTE_VERSION='6'" src\paintiano\05-main.jsx
  findstr /C:"kontra:'" src\paintiano\03-i18n.jsx
A bash check.sh can be generated on demand — every grep above with expected
counts; any divergence flags a silent build-pipeline regression.

=== ROLLOUT NOTE ===
PALETTE_VERSION bump means every user's saved palette is overwritten with the
new Scriabin seed on first open. This matches the v4→v5 rollout policy
documented in the prior force-seed comment. Pro users who had hand-picked
palettes will lose them — acceptable per the "every rollout resets" rule, but
worth flagging in release notes if you publish any.
